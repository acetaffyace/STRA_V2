# Offline pilot corpora

These three small corpora are development fixtures only. They are:

- `NOT HUMAN GOLD`
- `NOT PROVIDER OUTPUT`
- `origin = codex_offline_fixture`
- `workload_type = development_fixture`

They exist to exercise the complete local flow without an API key or paid
provider: ingest, labels, metric provenance, evidence, immutable results, and
the five-question contract.

