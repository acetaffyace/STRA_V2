These are deterministic unit-test fixtures only. They are not Steam source data,
not human-reviewed Golden Set records, and must not be used to claim model
quality. Real source review exports selected by `select_samples.py` are required
before P0.5b annotation.
