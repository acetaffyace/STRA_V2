# The desktop build consumes the same validated runtime dependency source as
# the API. Keep only the packaging tool local to this requirements file.
-r ../../api/requirements-core.txt
pyinstaller>=6.0
